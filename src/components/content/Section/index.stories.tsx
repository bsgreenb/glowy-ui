import React from "react";
import Section from ".";

export default {
  component: Section,
  title: "Content/Section",
};

export const Default = () => (
  <Section title="Hoops">Text in a section!</Section>
);
